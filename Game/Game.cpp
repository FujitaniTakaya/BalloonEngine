#include "stdafx.h" #include "Game.h"  // Load resources and set up your objects here (called once). // Per-frame logic goes here. // Your drawing code goes here. // K2EngineLow already cleared the screen to gray before this is called. 